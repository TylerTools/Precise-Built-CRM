# Database
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Auth
JWT_SECRET=

# Email — Resend (primary, recommended)
RESEND_API_KEY=

# Email — SMTP (fallback)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@precisebuilt.com

# Notifications
NOTIFICATION_EMAIL=tyler@precisebuilt.net

# File uploads — Vercel Blob (required for production)
BLOB_READ_WRITE_TOKEN=

# Google Drive (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
