# Database — Vercel Postgres (Neon) — same DB as marketing site
POSTGRES_PRISMA_URL="postgresql://..."
POSTGRES_URL_NON_POOLING="postgresql://..."

# JWT secret — must match the marketing site if sharing auth
JWT_SECRET="change-me-to-a-random-string-in-production"

# App URL — your CRM domain (for Google OAuth redirect)
NEXT_PUBLIC_APP_URL="https://crm.precisebui.lt"

# Google OAuth (optional)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# SMTP — email notifications
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@precisebui.lt"
NOTIFICATION_EMAIL="tyler@precisebui.lt"
